.search-bar-container
  .row
    .login-button-container
      = bootstrap_form_for :query, url: github_search_path do |f|
        .container-fluid
          .row
            .col-lg-5.col-lg-offset-3.col-md-5.col-md-offset-3.col-sm-5.col-sm-offset-3.col-xs-5.col-xs-offset-3
              = f.text_field :text, hide_label: true
            .col-lg-1.col-md-1.col-sm-1.col-xs-1
              = f.submit "Search"
.container
  .row
    #filter-container.col-lg-3.col-md-3.col-sm-3.col-xs-3.grey
      - if @result.present?
        = "#{@result.total_count} items found" 
    #results-container.col-lg-9.col-md-9.col-sm-9.col-xs-9.red
      - if @result.present?
        - @result[:items].each do |e|
          .row
            = e[:full_name]
